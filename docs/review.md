# 作品集交付审查

审查日期：2026-09-12。范围：当前源代码、测试、文档和两份内置演示资料。项目已发布到公开 GitHub 仓库；发布前审查未发现示例资料包含真实企业内部信息、个人信息或 API Key。

## 数据检查

- 游戏发行 Markdown 的完整正文均为本项目构造的访谈，明确标注虚构；只出现角色、地区和示例业务描述，没有具名联系人、电话号码、电子邮箱或真实交易编号。
- 消费品牌 Excel 的两个工作表均可见、明确标注虚构。检查了全部非空单元格，内容为访谈、角色与示例活动口径，没有个人联系方式或真实客户明细。
- 检查了 Excel ZIP 中的元数据与关联对象：无 core 作者元数据、无外部工作簿链接、无评论文件。原有 PNG 是该虚构工作簿的预览。
- 当前文本交付文件的常见密钥格式、手机号和邮箱格式扫描未发现匹配。`.env.example` 保持空密钥，当前根目录不存在 `.env`。
- 来源审阅与模式扫描不等同于完整 DLP 或历史秘密审计。后续加入材料、截图或录屏时，必须重新核验新增内容；虚构企业名可能与现实名称偶然重合，不代表使用其内部信息。

## 代码结构与修正

读取、证据检索、本地诊断、LLM 适配和 Markdown 渲染已经分层，适合此规模的原型。保留现有模块结构，本轮进行了以下修正：

- 页面证据与痛点标题进入 HTML 前进行转义，避免资料内容改变页面 HTML。
- 文件读取失败不再直接显示底层异常，而提示检查文件损坏、Excel 加密与 UTF-8 编码。
- 侧边栏修正为明确的资料使用与外发提示，移除与 LLM 行为矛盾的“不上传”承诺。
- `.gitignore` 扩展忽略环境文件变体和日志，明确保留空的 `.env.example`。

## 验证

`python -m pytest -q`：17 项测试通过。覆盖本地事实/证据、信息缺失、LLM JSON/证据拒绝、无密钥检查、文本与 Excel 读取、损坏与不支持的文件、Streamlit 本地生成及下载入口。

所有验证均未调用真实模型。没有用这些测试声称模型效果、业务收益、并发性能或生产安全性已获验证。

## 已知问题与展示边界

规则中仍有宽泛关键词，不能可靠区分否定、愿望与已发生问题；没有信号时的低分不代表已确认低风险。表格只转入有限行数，且示例 Excel 的展示标题会进入解析文本。证据摘录不含单元格坐标，LLM 证据匹配不验证结论语义。当前没有用户鉴权、调用配额或系统化模型评估。

README 已公开这些限制，录制脚本只演示实际支持的流程。视频和应用截图仍待录制，未填入假的演示链接或效果数据。
